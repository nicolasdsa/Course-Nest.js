const a: any = 22;
const b: any = ['Nicolas'];

const result = a + b;
console.log(result);

const formulario: { [campoFomulario: string]: any } = {
  nome: 'Nicolas',
  sobrenome: 'Machado',
  idade: 22,
};

console.log(formulario);
